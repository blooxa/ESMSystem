meta:
  environment: python
  version: "3.9"
  toolchain: pip
build:
  - pip install --no-cache-dir channels daphne
  - pip install --no-cache-dir -r requirements.txt
run:
  command: cd esm_backend && daphne -b 0.0.0.0 -p 8000 esm_backend.asgi:application
  port: 8000