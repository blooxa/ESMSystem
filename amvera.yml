---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.9
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: cd esm_backend && python manage.py runserver
  containerPort: 8000
  servicePort: 80
