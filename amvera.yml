---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.9
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  command: cd esm_backend && gunicorn esm_backend.wsgi:application --bind 0.0.0.0:8000
  containerPort: 8000
  servicePort: 80
